#include <iostream>
#include <vector>

bool isInArray(int **arr, int rows, int cols)
{
    return false;
}

int main()
{
    const int rows = 2;
    const int cols = 3;
    // int arr[2][3] = {1, 2, 3, 4, 5, 6};
    // int *b[5]; // surrogate
    // for (size_t i = 0; i < 5; ++i)
    // {
    //     b[i] = a[i];
    // }
    std::vector<std::vector<int>> arr2d{{1, 1, 1},
                                        {2, 2, 2}};
    arr2d.p

    bool res = false;

    // res = isInArray(arr, 2, 3);
    return 0;
}